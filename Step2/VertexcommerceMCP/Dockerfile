FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY server.py .

# Expose port and run
ENV PORT=8080
EXPOSE 8080

# Use 'uvicorn' to serve the FastMCP app
CMD ["python", "server.py"]